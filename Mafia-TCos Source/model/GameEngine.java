/*

    public boolean checkRequirements(Asset asset)
    {
        boolean available = false;
        int rank = playerUnique.getRank();
        int money = playerUnique.getMoney();

        if(asset.getRankThreshold() <= rank && asset.getPrice() <= money)
            available = true;
        return available;
    }

    public void purchaseAsset(Asset asset)
    {
        boolean available = checkRequirements(asset);
        if(available)
            playerUnique.getAssets().add(asset);
    }

*/



